@*@using SEDC.PizzaApp.Models.Domain*@
@*the data is sent to the view is of type List<Pizza>*@
@*@model List<Pizza>*@

@*<h2>Pizzas:</h2>
*@
@*Model is of type List<Pizza>*@
@*<ul>
    @for (int i = 0; i < @Model.Count; i++)
    {
        <li>@Model.ElementAt(i).Name</li>
    }

</ul>
*@
@using SEDC.PizzaApp.Models.ViewModels
@model List<PizzaViewModel>

    <ol>
    @for(int i=0; i<@Model.Count; i++)
    {
        <li>@Model.ElementAt(i).Id - @Model.ElementAt(i).Name - @Model.ElementAt(i).Price - @Model.ElementAt(i).PizzaSize</li>
    }
</ol>