@model List<Order>

<h2>List Of Orders:</h2>
<ul>
    @for (int i = 0; i < @Model.Count; i++)
    {
        <li>Order no.@Model.ElementAt(i).Id: @Model.ElementAt(i).Name, @Model.ElementAt(i).Size</li>
    }
</ul>